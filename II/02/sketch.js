var nombreEntier; /*création d'une variable souhaitée hors langague p5 */ 
var monTexte;

function setup() {
	createCanvas(windowWidth, windowHeight);

	background(190, 200, 240);

	nombreEntier = 10; /*definition de cette variable*/ 
	monTexte = "ahoj";

	print(nombreEntier + 30 );
	print(monTexte);

}
